server {
    listen 8080;  # Змінено порт на 8080
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
    }

    # Додаткові налаштування сервера
    # ...
}
